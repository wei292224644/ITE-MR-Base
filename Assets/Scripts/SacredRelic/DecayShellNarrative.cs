namespace MRBase.SacredRelic
{
    /// <summary>
    /// Single source of truth for what the awakening is allowed to destroy.
    ///
    /// Shell (crumbles, bursts, turns to powder): mud / silt crust, grime and soot,
    /// lichen or moss, and the weathered mineral hard-shell on the face.
    ///
    /// Core (stays): stone body, carved inscription, silhouette, and historical fracture
    /// lines that read as scars rather than as this sequence's blast.
    /// </summary>
    public static class DecayShellNarrative
    {
        public const string Summary =
            "Decay shell (mud, grime, lichen, weathered crust) peels away; " +
            "inscription core remains and restores.";
    }
}
